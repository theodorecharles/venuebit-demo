# Optimizely SDK Key (same for all platforms)
# Get this from your Optimizely Feature Experimentation project
OPTIMIZELY_SDK_KEY=your_sdk_key_here

# Datafile polling interval in milliseconds
# - Not set or empty: polling disabled, use webhook for updates
# - -1: polling disabled, use webhook for updates
# - Positive number (e.g., 1000): poll every N ms, webhook is ignored
POLLING_INTERVAL=
